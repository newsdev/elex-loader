DROP TABLE IF EXISTS ballot_positions CASCADE; CREATE TABLE ballot_positions(
    id varchar,
    unique_id varchar,
    candidateid varchar,
    ballotorder integer,
    description varchar,
    last varchar,
    polid varchar,
    polnum varchar,
    seatname varchar
);