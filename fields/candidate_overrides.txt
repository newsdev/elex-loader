DROP TABLE IF EXISTS override_candidates CASCADE; CREATE TABLE override_candidates(
    candidate_candidateid varchar,
    nyt_candidate_name varchar,
    nyt_candidate_important bool,
    nyt_candidate_description text,
    nyt_races integer[],
    nyt_display_order integer,
    nyt_winner bool,
    nyt_delegates numeric
);