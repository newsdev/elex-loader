DROP TABLE IF EXISTS candidates CASCADE; CREATE TABLE candidates(
    id varchar,
    candidateid varchar,
    ballotorder integer,
    first varchar,
    last varchar,
    party varchar,
    polid varchar,
    polnum varchar
);